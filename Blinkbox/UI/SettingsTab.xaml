<UserControl x:Class="GitScc.Blinkbox.UI.SettingsTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="130" />
            <ColumnDefinition Width="126" />
            <ColumnDefinition Width="668" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="#FFC5E8F8" Offset="1" />
                <GradientStop Color="White" Offset="0" />
            </LinearGradientBrush>
        </Grid.Background>

        <Label Name="TestSwarmUsernameLabel" Content="TestSwarm username:" Grid.Row="0"  />
        <Label Name="TestSwarmPasswordLabel" Content="TestSwarm password:" Grid.Row="1" />
        <Label Name="TabTestSwarmTagsLabel" Content="TestSwarm tags:" Grid.Row="2" />
        <Label Name="TabTestSwarmUrl" Content="TestSwarm tags:" Grid.Row="3" />

        <TextBox Name="testSwarmUsername" Grid.Row="0" Grid.Column="1" 
                             Text="{Binding Path=solutionUserSettings.TestSwarmUsername}" />
        <TextBox Name="testSwarmPassword"  Grid.Row="1" Grid.Column="1" 
                             Text="{Binding Path=solutionUserSettings.TestSwarmPassword}" />
        <TextBox Name="testSwarmTags" Grid.Row="2" Grid.Column="1" 
                             Text="{Binding Path=solutionUserSettings.TestSwarmTags}" />
        <TextBox Name="testSwarmUrl" Grid.Row="3" Grid.Column="1" 
                             Text="{Binding Path=solutionSettings.TestSwarmUrl}" />
    </Grid>
</UserControl>
